package com.thilinam.model;
/**
 * <p>
 *     This contains the data models which are going to persist into the producer in order to consume and
 *     persist on to the database.
 *     These models map data with the IOT device byte array but not contain any database persist entities
 * </p>
 */